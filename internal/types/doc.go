// Package types holds pure-data domain types.
//
// It imports nothing internal. Behavior (DB queries, business rules, HTTP
// DTOs) lives in other packages. The structs and enums declared here are
// shared across the storage, service, and transport layers.
package types
